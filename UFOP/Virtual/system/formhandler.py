def HandleForm(*payload):
    # We'd sanitize our variables here, and do one function for each kind of form. This is a small project, so we'll pretend we did it.
    # It would return False if something was wrong, we'd handle it, etcetera.
    return True